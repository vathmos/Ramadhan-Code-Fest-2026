# Conway's Game of Life

Implementasi [Conway's Game of Life](https://en.wikipedia.org/wiki/Conway%27s_Game_of_Life) menggunakan Go dengan render menggunakan Braille characters di terminal.

## Run

```bash
go run .
```

Tekan `Ctrl+C` untuk keluar.
